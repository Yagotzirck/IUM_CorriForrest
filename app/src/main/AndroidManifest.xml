<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.altervista.yagotzirck.corriforrest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CorriForrest">

        <activity
            android:name=".CreateGoalActivity"
            android:label="@string/toolbar_createNewGoal">
        </activity>

        <activity
            android:name=".SessionRecapActivity"
            android:label="@string/toolbar_sessionRecap"
            />

        <activity android:name=".SessionStartedActivity"
            android:label="@string/toolbar_sessionStarted_status"/>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.CorriForrest.NoActionBar"
            android:label="@string/toolbar_startSession"/>
        <activity
            android:name=".SignupActivity"
            android:theme="@style/Theme.CorriForrest.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/Theme.CorriForrest.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyAOKPdyX8zvzVh5JU6b5dxgMNsHqHI6fKU" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />
    </application>

</manifest>